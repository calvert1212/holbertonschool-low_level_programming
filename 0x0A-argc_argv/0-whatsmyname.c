#include <stdio.h>

/**
 * main - Prints name of program
 * @argc: the argument
 * @argv: the argument array
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
